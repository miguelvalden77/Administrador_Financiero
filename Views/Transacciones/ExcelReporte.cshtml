@{
    ViewData["Title"] = "Reporte excel";

    var hoy = DateTime.Today;
    var haceUnMes = hoy.AddMonths(-1);
    var mesActual = hoy.Month;
    var añoActual = hoy.Year;
    var mesAnterior = haceUnMes.Month;
    var mesAnteriorAño = haceUnMes.Year;
    var añoPasado = hoy.AddYears(-1);
}

<partial name="_Submenu" model="SubMenuTransacciones.Excel"/>

<h1>Exportar transacciones a Excel</h1>

<div class="mb-3">
    <a class="btn btn-success" asp-action="ExportarExecelPorMes" asp-route-mes="@mesActual" asp-route-año="@añoActual">
        Exportar mes actual
    </a> - @hoy.ToString("MMMM yyyy")
</div>

<div class="mb-3">
    <a class="btn btn-success" asp-action="ExportarExecelPorMes" asp-route-mes="@mesAnterior" asp-route-año="@mesAnteriorAño">
        Exportar mes anterior
    </a> - @haceUnMes.ToString("MMMM yyyy")
</div>


<div class="mb-3">
    <a class="btn btn-success" asp-action="ExportarExcelPorAño" asp-route-año="@añoActual">
        Exportar año actual
    </a> - @hoy.ToString("yyyy")
</div>

<div class="mb-3">
    <a class="btn btn-success" asp-action="ExportarExcelPorAño" asp-route-año="@añoActual">
        Exportar año anterior
    </a> - @añoPasado.ToString("yyyy")
</div>

<div class="mb-3">
    <a class="btn btn-success" asp-action="ExportarTodoExcel">
        Exportar todo
    </a>
</div>